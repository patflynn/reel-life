{
  "statusLine": {
    "command": "input=$(cat); cwd=$(echo \"$input\" | jq -r '.workspace.current_dir'); branch=$(git -C \"$cwd\" --no-optional-locks branch --show-current 2\u003e/dev/null); if [ -n \"$branch\" ]; then echo \"reel-life ($branch)\"; else echo \"reel-life\"; fi",
    "type": "command"
  }
}
